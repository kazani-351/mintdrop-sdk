#!/usr/bin/env sh

pnpm install
pnpm changeset
pnpm changeset version
pnpm clean
pnpm generate # generate after version
pnpm build # build again to ensure generated files
pnpm --filter "@mintdrop/*" publish
